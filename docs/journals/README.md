# Per-VLA bring-up journals

Short markdown journals from each VLA's *initial* setup session.
These are NOT the cumulative research journal — that lives at
`<repo>/journal.md` (top-level, ~180 KB, append-only, tagged by
`[policy=...]` per attempt).

| File | What |
|---|---|
| `grootn1.7-bringup.md` | GR00T N1.7 first-time setup + Cosmos-Reason2 HF-auth quirks |
| `dreamzero-bringup.md` | DreamZero (NVIDIA GEAR) Modal Tunnels exploration |

For real per-attempt logs, see the top-level `journal.md`.
